<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <groupId>org.openhab.addons.osgiify</groupId>
  <artifactId>org.openhab.addons.reactor.osgiify</artifactId>
  <version>2.5.0-SNAPSHOT</version>

  <packaging>pom</packaging>

  <name>openHAB Add-ons :: OSGi-ify</name>
  <description>This project contains OSGi-ified versions of openHAB dependencies</description>

  <organization>
    <name>openHAB.org</name>
    <url>http://www.openhab.org</url>
  </organization>

  <licenses>
    <license>
      <name>Eclipse Public License 2.0</name>
      <url>https://www.eclipse.org/legal/epl-2.0/</url>
    </license>
  </licenses>

  <modules>
    <module>com.github.calimero.calimero-core</module>
    <module>de.kaizencode.tchaikovsky</module>
  </modules>

  <scm>
    <connection>scm:git:https://github.com/openhab/openhab-osgiify.git</connection>
    <developerConnection>scm:git:https://github.com/J-N-K/openhab-osgiify.git</developerConnection>
    <tag>HEAD</tag>
    <url>https://github.com/J-N-K/openhab-osgiify</url>
  </scm>

  <issueManagement>
    <system>Github</system>
    <url>https://github.com/J-N-K/openhab-osgiify/issues</url>
  </issueManagement>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <oh.java.version>1.8</oh.java.version>
    <maven.compiler.source>${oh.java.version}</maven.compiler.source>
    <maven.compiler.target>${oh.java.version}</maven.compiler.target>
    <maven.compiler.compilerVersion>${oh.java.version}</maven.compiler.compilerVersion>

    <bnd.version>4.2.0</bnd.version>
    <karaf.version>4.2.4</karaf.version>

    <osgiify.import></osgiify.import>
    <osgiify.export></osgiify.export>
    <osgiify.instructions></osgiify.instructions>
    <osgiify.includeresource></osgiify.includeresource>
    <osgiify.classpath>target/lib/wrap-this.jar</osgiify.classpath>
  </properties>

  <repositories>
    <repository>
      <releases>
        <enabled>true</enabled>
        <updatePolicy>never</updatePolicy>
      </releases>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
      <id>jcenter</id>
      <name>JCenter Repository</name>
      <url>https://jcenter.bintray.com</url>
    </repository>

    <repository>
      <releases>
        <enabled>true</enabled>
        <updatePolicy>never</updatePolicy>
      </releases>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
      <id>openhab-artifactory-release</id>
      <name>JFrog Artifactory Repository</name>
      <url>https://openhab.jfrog.io/openhab/libs-release</url>
    </repository>
  </repositories>

  <pluginRepositories>
    <pluginRepository>
      <id>jcenter</id>
      <url>https://jcenter.bintray.com</url>
    </pluginRepository>
    <pluginRepository>
      <id>openhab-artifactory-release</id>
      <url>https://openhab.jfrog.io/openhab/libs-release</url>
    </pluginRepository>
  </pluginRepositories>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jar-plugin</artifactId>
          <configuration>
            <archive>
              <manifestFile>${project.build.outputDirectory}/META-INF/MANIFEST.MF</manifestFile>
            </archive>
            <skipIfEmpty>true</skipIfEmpty>
          </configuration>
        </plugin>
        <plugin>
          <artifactId>maven-dependency-plugin</artifactId>
          <executions>
            <execution>
              <id>copy-libs</id>
              <phase>generate-sources</phase>
              <goals>
                <goal>copy</goal>
              </goals>
              <configuration>
                <artifactItems>
                  <artifactItem>
                    <groupId>${osgiify.groupId}</groupId>
                    <artifactId>${osgiify.artifactId}</artifactId>
                    <version>${project.version}</version>
                    <type>jar</type>
                    <outputDirectory>target/lib</outputDirectory>
                    <destFileName>wrap-this.jar</destFileName>
                    <overWrite>true</overWrite>
                  </artifactItem>
                </artifactItems>
              </configuration>
            </execution>
          </executions>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <plugin>
        <groupId>biz.aQute.bnd</groupId>
        <artifactId>bnd-maven-plugin</artifactId>
        <version>${bnd.version}</version>
        <configuration>
          <bnd><![CDATA[
Bundle-SymbolicName: ${project.artifactId}
Bundle-Description: OSGi-ified version of ${osgiify.name}
Bundle-Name: ${osgiify.name}
Bundle-Version: ${project.version}
Bundle-License: ${osgiify.license}
Automatic-Module-Name: ${def;bsn}
Import-Package: \\
  ${osgiify.import},\\
  *
Export-Package: \\
  ${osgiify.export}, \\
  *;version=${project.version}
-includeresource: NOTICE, \\
  ${osgiify.includeresource}
-classpath: ${osgiify.classpath}
${osgiify.instructions}]]></bnd>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>bnd-process</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  
  <profiles>
    <profile>
      <id>local-library</id>
      <activation>
        <file>
          <exists>lib/</exists>
        </file>
      </activation>
      <build>
        <pluginManagement>
          <plugins>
            <plugin>
              <artifactId>maven-dependency-plugin</artifactId>
              <executions>
                <execution>
                  <id>copy-libs</id>
                  <phase>none</phase>
                </execution>
              </executions>
            </plugin>
          </plugins>
        </pluginManagement>
      </build>
    </profile>
    <profile>
      <id>add-contrib</id>
      <activation>
        <file>
          <exists>contrib/</exists>
        </file>
      </activation>
      <properties>
        <osgiify.includeresource>${.}/contrib/</osgiify.includeresource>
      </properties>
    </profile>
  </profiles>
  
</project>
